package com.kdresdell.iptvtv.phone.theme

import androidx.compose.ui.graphics.Color

// Black background like the TV app, with the launcher icon's colors as the
// accents: IconYellow is its background (res/values/colors.xml
// ic_launcher_background), HatBrown is the cap in ic_launcher_foreground.png.
val IconYellow = Color(0xFFECC256)
val HatBrown = Color(0xFF90604A)

// Dark brown for text/icons sitting on the yellow (buttons, selected items).
val DeepBrown = Color(0xFF2A1A10)

// Warm near-blacks rather than pure grey, so the yellow doesn't look pasted on.
val Background = Color(0xFF0C0B0A)
val SurfaceContainer = Color(0xFF1C1A17)
val SurfaceContainerHigh = Color(0xFF2A2723)
val SurfaceContainerHighest = Color(0xFF36322D)
val OnSurface = Color(0xFFEDE7DD)
val OnSurfaceVariant = Color(0xFFB8AE9E)
val Outline = Color(0xFF6E665B)
val OutlineVariant = Color(0xFF2E2B27)
